#include "main.h"
#include <stdio.h>

/**
 * *_memset - Functions that fills memory wh a constant byte
 * @s: pointer to memory area to fill
 * @n: the number of bytes to fill
 * @b: constant byte
 * Return: a pointer to the memory area @s
